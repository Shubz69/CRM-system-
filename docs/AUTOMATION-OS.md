# Automation OS

**Status:** Spec (Phase 8). Foundation: AutomationRule, Autopilot, FollowUps.

Trigger → Conditions → Agent/Logic steps → Actions → Approval → Outcome.

Support NL setup that compiles to a **visible** workflow before enable.

Outbound messaging/publishing remain approval-gated by default.
