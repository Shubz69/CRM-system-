# AI Operator (Claude)

Configured via `AI_PROVIDER` (default **`anthropic`**):

- `anthropic` (`ANTHROPIC_API_KEY`) — **primary / required for production AI**
- `mock` — local/demo only
- `openai` (`OPENAI_API_KEY`) — **optional adapter only; never required**

See [AI_CLAUDE.md](./AI_CLAUDE.md) for model routing, structured decisions, and Autopilot.
